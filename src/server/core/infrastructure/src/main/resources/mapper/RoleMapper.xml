<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mayanshe.scrmstd.infrastructure.persistence.mapper.RoleMapper">
    <!-- 租户角色(roles)表映射 -->

    <insert id="insert" parameterType="RolePo" useGeneratedKeys="false">
        INSERT INTO roles (id, tenant_id, role_name, description, created_at)
        VALUES (#{id}, #{tenantId}, #{roleName}, #{description}, #{createdAt})
    </insert>

    <update id="update" parameterType="RolePo">
        UPDATE roles
        SET role_name   = #{roleName},
            description = #{description},
            updated_at  = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE
        FROM roles
        WHERE id = #{id} and role_name != '超级管理员'
    </delete>

    <select id="findByIds" parameterType="list" resultType="RolePo">
        SELECT *
        FROM roles
        WHERE id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>